- name: Google Chrome up to date (macOS)
  platform: darwin
  description: Checking to make sure Google Chrome is up to date.
  resolution: Run all available updates for Google Chrome.
  query: SELECT 1 FROM apps WHERE name = 'Google Chrome.app' AND version_compare(bundle_short_version, '127.0.6668.38') >= 0;
